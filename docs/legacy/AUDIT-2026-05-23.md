# Architectural Audit — 2026-05-23

> **Status:** Mostly resolved. Items 1–8 from the original audit are all closed.
> Remaining: TUI rewrite (ROADMAP item) and two CONCERNs for future consideration.
> This file can be deleted once the TUI rewrite is complete.

---

## ✅ Resolved Items

### LIE-01 — `ExecutionModes.ts` does not exist

**RESOLVED.** `src/orchestrator/ExecutionModes.ts` created (commit dadd1e6) with
AUTOPILOT/ECOMODE/PIPELINE/RALPH handlers + `routeToHandler()` stub in `Orchestrator.ts`.
Gap 1 in `architectural-decisions.md` is now truly closed.

---

### LIE-02 — BUG-04 is already fixed in code but still listed as open

**RESOLVED.** BUG-04 closed in STATUS.md and ROADMAP.md. `permanently_disconnected` event
emitted via `FedClient.emit('permanently_disconnected', { url, attempts })` when max
reconnect attempts (12) are reached.

---

### LIE-03 — BUG-02 regression: STATUS.md vs loop-decisions.md conflict

**RESOLVED.** `fixMeow()` in `src/agent/agent.ts` uses `exec()` with `@file` syntax, not
`spawnSync`/`execSync`. Both STATUS.md and loop-decisions.md now agree the fix is in place.

---

### LIE-04 — BUG-07 partially improved but STATUS still describes old behavior

**RESOLVED.** BUG-07 closed in STATUS.md and ROADMAP.md. Fallback now exits 1 explicitly
when no test file exists: `node -e "console.error('ERROR: No test file found...');process.exit(1)"`.

---

### CONFIRMED: BUG-01 — `vec_memory` integer PK crash

**RESOLVED.** `src/agent/memory.ts` and `src/kernel/database.ts` now use `vec0_insert()`
from sqlite-vec API instead of manual rowid insertion. Commit ca20ee4.

---

### CONFIRMED: BUG-05 — `FileCoordinator.requestAccess()` result ignored

**RESOLVED.** `src/orchestrator/ParallelExecutor.ts` now checks `allowed` before dispatch.
If false, task is requeued with exponential backoff (50ms × 2^attempts, capped at 1000ms).

---

### CONFIRMED: BUG-06 — PID mismatch on respawn

**RESOLVED.** `src/kernel/kernel.ts` `respawnAgent()` now returns `Promise<number | null>`.
All callers update the mission registry with the new PID.

---

### MISSING-01 — Harvester-on-failure-path work (commit 7bbff2a) not in Completed

**RESOLVED.** Added to STATUS.md Completed table.

---

## 🟡 Partially Resolved / Not Bugs

### CONCERN-01 — Quantum features: no evidence they function

**NOT VERIFIED.** The memory subsystem is now fixed (BUG-01 resolved) so Grover's search
_should_ work. However no live test was run. Before claiming these as architectural
strengths in external context, run a live Grover search test and record in
`docs/rfc/evidence-report.md`.

**Status:** Low priority — not blocking anything.

---

### CONCERN-02 — Tier-based cost control (Haiku/Sonnet/Opus) still missing

**NOT STARTED.** No bug filed. The comparison table in `architectural-decisions.md`
shows this as a gap meow lacks. If cost control matters for the project direction,
add to ROADMAP.md as a future wave item.

**Status:** Future consideration, not a bug.

---

### CONCERN-03 — No `TaskEvents` observer wiring

**NOT STARTED.** `architectural-decisions.md` listed "TaskEvents are stubs" as a weakness
but never converted it to a tracked gap or bug. `emitTaskStart`/`emitTaskComplete`
wiring in Orchestrator is unverified.

**Status:** Low priority audit task.

---

## 📋 Remaining ROADMAP Item

**TUI rewrite** — `docs/rfc/tui-spec.md` describes a task-tree + streaming output
redesign. The only remaining Wave 4 item. Pick this after all criticals done.

---

*Written: 2026-05-23. Updated: 2026-05-24 to reflect LIE-01 through LIE-04 and BUG-01/05/06 resolved (commit dadd1e6).*